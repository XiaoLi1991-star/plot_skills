# Legacy shim for copied scripts that still source scripts/lib/rplot_common.R.
suppressPackageStartupMessages({
  library(data.table)
  library(ggplot2)
  library(readxl)
  library(scales)
  library(codex.rplot)
})
